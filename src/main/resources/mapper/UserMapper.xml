<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.boards.mapper.UserMapper">

    <insert id="insertUser">
        INSERT INTO users (user_id, password, name, email)
        VALUES (#{userId}, #{password}, #{name}, #{email})
    </insert>

    <select id="findByUserId" resultType="User">
        SELECT user_id, password, name, email, email_verified, email_verified_at, created_at, password_changed_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findByEmail" resultType="User">
        SELECT user_id, password, name, email, email_verified, email_verified_at, created_at, password_changed_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findByUserIdAndPassword" resultType="User">
        SELECT user_id, password, name, email, email_verified, email_verified_at, created_at, password_changed_at
        FROM users
        WHERE user_id = #{userId} AND password = #{password}
    </select>

    <update id="updatePassword">
        UPDATE users
        SET password = #{password}, password_changed_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
    </update>

    <update id="updateEmailVerified">
        UPDATE users
        SET email_verified = TRUE, email_verified_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
    </update>

</mapper>
