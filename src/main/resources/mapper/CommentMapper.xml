<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.boards.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="Comment">
        <id property="commentId" column="comment_id"/>
        <result property="postId" column="post_id"/>
        <result property="authorId" column="author_id"/>
        <result property="authorName" column="author_name"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByPostId" resultMap="CommentResultMap">
        SELECT c.comment_id, c.post_id, c.author_id, u.name as author_name,
               c.content, c.created_at, c.updated_at
        FROM comments c
        JOIN users u ON c.author_id = u.user_id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>

    <select id="findById" resultMap="CommentResultMap">
        SELECT c.comment_id, c.post_id, c.author_id, u.name as author_name,
               c.content, c.created_at, c.updated_at
        FROM comments c
        JOIN users u ON c.author_id = u.user_id
        WHERE c.comment_id = #{commentId}
    </select>

    <insert id="insertComment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comments (post_id, author_id, content)
        VALUES (#{postId}, #{authorId}, #{content})
    </insert>

    <update id="updateComment">
        UPDATE comments
        SET content = #{content}
        WHERE comment_id = #{commentId}
    </update>

    <delete id="deleteComment">
        DELETE FROM comments WHERE comment_id = #{commentId}
    </delete>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM comments WHERE post_id = #{postId}
    </select>

</mapper>
